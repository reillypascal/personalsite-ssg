---
title: Reilly Spitzfaden, Composer | Notes
description: Short posts, photoblogging, and interesting things from around the web—it's like Twitter meets Instagram, but I own all my posts!
pagination:
    data: collections.note
    size: 10
    alias: notes
    reverse: true
---

<link rel="stylesheet" type="text/css" href="/styles/blog.css" />
<link rel="stylesheet" type="text/css" href="/styles/blogposts.css" />
<link rel="stylesheet" type="text/css" href="/styles/onecolumn.css" />

<h1 class="sectionHeader">Notes</h1>

<section class="postFeedDescription">
    <p>Short posts, photoblogging, and interesting things from around the web—it's like Twitter meets Instagram, but I own all my posts!</p>

    {% include "partials/post-list-links.liquid" %}
</section>

{% for note in notes %}
<article class="h-entry">
    <h2 class="p-name"><a class="u-url" href="{{note.url}}">{{ note.data.title }}</a></h2>

    <a rel="author" class="p-author h-card" href="https://reillyspitzfaden.com"></a>

    <div class="e-content">
        {{ note.content }}
    </div>
    <div class="post-meta">
        <span class="meta-icon">
            <img src="/media/icon-calendar-ltr.svg" alt="calendar icon" />
            </span>
            <span class="meta-text">
            <time class="dt-published" datetime="{{ note.date }}">{{ note.date | date: "%A, %B %d, %Y" }}</time>
        </span>
    </div>
    {% if note.data.location and note.data.location != "" %}
        <div class="post-loc">
            <span class="loc-icon">
                <img src="/media/ui/map-pin-icon-light-18px.svg" alt="map pin icon" width="18" height="18" />
            </span>
            <span class="loc-text">
                <span>{{ note.data.location }}</span>
            </span>
        </div>
    {% endif %}
</article>
{% endfor %}

<a class="RSSValidationLink" href="https://validator.w3.org/feed/check.cgi?url=https%3A//reillyspitzfaden.com/notes/feed.xml"><img src="/media/valid-rss-rogers.png" alt="[Valid RSS]" title="Validate my RSS feed" class="RSSValidationImg"/></a>

<div class="page-nav-wrapper">
    {% if pagination.href.previous %}
        <a href="{{pagination.href.previous}}">Previous Page</a>
    {% endif %}
    {% if pagination.href.next %}
        <a href="{{pagination.href.next}}">Next Page</a>
    {% endif %}
</div>

