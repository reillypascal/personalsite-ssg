---
layout: mainlayout.liquid
title: Reilly Spitzfaden, Composer | Posts by Tag
description: Blog posts listed by tag
canonical_url: https://reillyspitzfaden.com/tags
styles:
    - onecolumn.css
---
<style>
    figure {
        margin-left: 20px;
    }
    .tag {
        margin: 20px 0;
    }
</style>
<main>
    <h2 class="sectionHeader">All tags</h2>
    <div id="allTagsList">
        {% for tag in collections.allTags %}
            {% if tag != 'post' %}
            {% if tag != 'like' %}
            <figure>
                <p class="tag" id="{{ tag }}"><a href="/tags/{{ tag }}">#{{ tag }}</a> ({{ collections[tag].length }}) </p>
            </figure>
            {% endif %}
            {% endif %}
        {% endfor %}
        
    </div>
</main>

{% comment %} <link rel="stylesheet" type="text/css" href="/styles/onecolumn.css" /> {% endcomment %}

