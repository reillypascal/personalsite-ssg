---
layout: mainlayout.liquid
title: Reilly Spitzfaden, Composer | Blog
description: A blog about my personal interests, including composition/sound design in Max/MSP, C++ audio plugin development with JUCE, the IndieWeb, and web development
canonical_url: https://reillyspitzfaden.com/blog
styles:
    - onecolumn.css
pagination:
    data: collections.post
    size: 8
    alias: posts
    reverse: true
eleventyExcludeFromCollections: true
---
<link rel="stylesheet" type="text/css" href="/styles/onecolumn.css" />

<style>
    p {
        line-height: var(--p-line-height);
    }
    #search {
        padding-top: 8px;
    }
    @media screen and (min-width: 480px) {
        #search {
            padding-top: 12px;
        }
    }
    @media screen and (min-width: 640px) {
        #search {
            padding-top: 16px;
        }
    }
    .page-nav-wrapper {
        display: flex;
        justify-content: space-between;
    }
    .post-tags {
        line-height: var(--p-line-height);
        margin: 16px 0;
        font-size: 0.95em;
    }
    .post-tag {
        margin-right: 4px;
        color: var(--fg-med-light);
    }
    .RSSValidation {
        margin-bottom: 24px;
    }
</style>
    
<main>
    <!-- blog contents -->
    <h2 class="sectionHeader">Blog</h2>
    <p>A blog about my personal interests, including composition and sound design; audio development using <a href="https://en.wikipedia.org/wiki/Max_(software)">Max/MSP</a>, C++, <a href="https://juce.com/">JUCE</a>, and Rust; and web development on the <a href="https://indieweb.org/">IndieWeb</a></p>
    
    <p><a href="/tags">View all tags</a></p>
    
    <link href="/pagefind/pagefind-ui.css" rel="stylesheet">
    <style>
        .pagefind-ui {
            --pagefind-ui-primary: var(--fg-light);
            --pagefind-ui-text: var(--fg-light);
            --pagefind-ui-background: var(--bg-dark);
            --pagefind-ui-border: var(--accent-med-light);
            --pagefind-ui-tag: var(--accent-med-light);
            --pagefind-ui-font: 'Open Sans', sans-serif;
        }
        ::placeholder {
            color: var(--fg-light) !important;
            opacity: 0.5 !important;
        }
        ::-ms-input-placeholder { /* Edge 12 -18 */
            color: var(--fg-light) !important;
        }
        .pagefind-ui__result-thumb {
            display: none;
        }
    </style>
    <script src="/pagefind/pagefind-ui.js"></script>
    <div id="search"></div>
    <script>
        window.addEventListener('DOMContentLoaded', (event) => {
            new PagefindUI({ element: "#search", showSubResults: true });
        });
    </script>
        
    {% for post in posts %}
        <article>
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <p>{{ post.data.description }}</p>
            <span class="meta-icon">
                <img src="/media/icon-calendar-ltr.svg" alt="calendar icon" width="18" height="18" />
            </span>
            <span class="meta-text">
                <time>{{ post.date | date: "%A, %B %d, %Y" }}</time>
            </span>

            {% comment %} <div class="post-tags">
                {% assign sorted_categories = post.data.tags | sort %}
                {% for tag in sorted_categories %}
                    {% if tag != 'post' %}
                        <span class="post-tag"><a href="/tags/{{ tag }}">#{{ tag }}</a></span>
                    {% endif %}
                {% endfor %}
            </div> {% endcomment %}
        </article>
    {% endfor %}

    <a href="https://validator.w3.org/feed/check.cgi?url=https%3A//reillyspitzfaden.com/blog/feed.xml"><img src="/media/valid-rss-rogers.png" alt="[Valid RSS]" title="Validate my RSS feed" class="RSSValidation"/></a>
    
    <div class="page-nav-wrapper">
        {% if pagination.href.previous %}
            <a href="{{pagination.href.previous}}">Previous Page</a>
        {% endif %}
        {% if pagination.href.next %}
            <a href="{{pagination.href.next}}">Next Page</a>
        {% endif %}
    </div>
</main>
